---
description: 共享类型与 API 契约规范
globs: ["shared/**", "frontend/src/lib/api-types.ts", "scripts/sync-api-types.sh"]
---

# 共享类型与 API 契约规范

## 核心原则
后端 FastAPI 是所有 API 类型的**唯一真实来源**。
前端 TypeScript 类型从 OpenAPI spec **自动生成**。

## 工作流
1. 后端开发者在 `backend/app/schemas/` 定义 Pydantic schemas
2. FastAPI 自动暴露 `/openapi.json`
3. 运行 `./scripts/sync-api-types.sh` 生成 `frontend/src/lib/api-types.ts`
4. 前端从 `api-types.ts` 导入类型 — 禁止手动定义 API 类型

## 规则
- `frontend/src/lib/api-types.ts` 是自动生成的 — 禁止手动编辑
- 如果需要修改 API 类型，先修改后端的 Pydantic schema
- 修改任何后端 schema 后，提醒用户运行同步脚本
- `shared/types/` 可以包含前后端共享的非 API 类型（例如枚举、常量）

## API 客户端模式
```typescript
// frontend/src/lib/api.ts
const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";

export async function apiFetch<T>(
  path: string,
  options?: RequestInit
): Promise<T> {
  const res = await fetch(`${API_BASE}${path}`, {
    headers: { "Content-Type": "application/json", ...options?.headers },
    ...options,
  });
  if (!res.ok) {
    const error = await res.json().catch(() => ({}));
    throw new ApiError(res.status, error.detail || "Unknown error");
  }
  return res.json();
}
```
